generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Lead {
  id               String        @id @default(cuid())
  name             String
  title            String?
  company          String?
  email            String?
  phone            String?
  serviceType      String        @default("ODOO_CONSULTING") // ODOO_CONSULTING, TRAINING, ADVISORY
  stage            String        @default("NEW_INQUIRY") // NEW_INQUIRY, DISCOVERY_CALL, PROPOSAL_SENT, NEGOTIATION, WON_ACTIVE, COMPLETED, LOST
  dealValue        Float         @default(0.0)
  currency         String        @default("BDT")
  probability      Int           @default(30)
  priority         String        @default("MEDIUM") // HIGH, MEDIUM, LOW
  aiScore          Int?
  aiPriorityReason String?
  aiDossier        String?
  notes            String?
  tags             String?       // comma-separated tags e.g. "Odoo 18, Accounting, Training Batch 4"
  lastContactDate  DateTime?
  nextFollowUpDate DateTime?
  nextFollowUpGoal String?
  createdAt        DateTime      @default(now())
  updatedAt        DateTime      @updatedAt

  meetings         Meeting[]
  milestones       Milestone[]
  activities       ActivityLog[]
  attachments      Attachment[]
}

model Meeting {
  id           String    @id @default(cuid())
  title        String
  leadId       String?
  lead         Lead?     @relation(fields: [leadId], references: [id], onDelete: Cascade)
  startTime    DateTime
  endTime      DateTime
  locationType String    @default("ZOHO_MEETING") // ZOHO_MEETING, GOOGLE_MEET, WHATSAPP_CALL, IN_PERSON
  meetUrl      String?
  zohoEventId  String?
  status       String    @default("SCHEDULED") // SCHEDULED, COMPLETED, CANCELLED
  rawNotes     String?
  aiSummary    String?
  actionItems  String?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}

model Milestone {
  id            String    @id @default(cuid())
  leadId        String
  lead          Lead      @relation(fields: [leadId], references: [id], onDelete: Cascade)
  title         String
  amount        Float     @default(0.0)
  currency      String    @default("BDT")
  status        String    @default("PENDING") // PENDING, INVOICED, PAID
  dueDate       DateTime?
  paidDate      DateTime?
  invoiceNumber String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

model ActivityLog {
  id        String   @id @default(cuid())
  leadId    String
  lead      Lead     @relation(fields: [leadId], references: [id], onDelete: Cascade)
  type      String   // NOTE, WHATSAPP_SENT, MEETING_HELD, STAGE_CHANGED, AI_SCORED, HERMES_SYNC
  content   String
  metadata  String?  // JSON formatted data
  createdAt DateTime @default(now())
}

model Setting {
  id        String   @id @default(cuid())
  key       String   @unique
  value     String
  updatedAt DateTime @updatedAt
}

model Attachment {
  id        String   @id @default(cuid())
  leadId    String
  lead      Lead     @relation(fields: [leadId], references: [id], onDelete: Cascade)
  fileName  String
  fileType  String
  fileSize  Int
  fileData  String
  createdAt DateTime @default(now())
}

